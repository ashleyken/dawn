{
"products": [
	{%- for product in collection.products -%}
	{"title": {{ product.title | remove: product.vendor | remove: "Car Mats" | json }}, "handle":{{ product.handle | json }}}
	{%- unless forloop.last %},{%- endunless -%}
	{%- endfor -%}
]
}