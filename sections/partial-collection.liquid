{
"products": [
	{%- for product in collection.products -%}
	{"title": {{ product.title | json }}, 'handle':{{ product.handle | json }}}
	{%- unless forloop.last %},{%- endunless -%}
	{%- endfor -%}
]
}